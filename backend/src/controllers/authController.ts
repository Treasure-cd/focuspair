import { getUserByIdentifier, createUser } from "../models/authModel.js";
import { createError } from "../utils/createError.js";
import type { HandlerTypes } from "../types/handlerTypes.js";

export const createUserController = async({req, res, next}: HandlerTypes) => {
    
}